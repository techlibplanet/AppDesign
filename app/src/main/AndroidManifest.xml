<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="appdesign.example.com.appdesign">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".UserInfoActivity" />
        <activity android:name=".DashboardActivity" />
        <activity android:name=".WalletActivity" />
        <activity android:name=".PlayActivity" />
        <activity android:name=".AddPoints" />
        <activity android:name=".TransferPoints" />
        <activity android:name=".WithdrawalPoints" />
        <activity android:name=".QuizActivity" />
        <activity android:name=".policies.PoliciesActivity" />
        <activity android:name=".ResultScreen" />
        <activity android:name=".settings.SettingsActivity" />
        <activity android:name=".MultiplayerQuizMenu" />
        <activity android:name=".profile.ProfileActivity"></activity>
    </application>

</manifest>